% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fingerprints.R
\name{CalculateRdkitFingerprints}
\alias{CalculateRdkitFingerprints}
\title{Calculate RDKit topological fingerprints for a set of molecules}
\usage{
CalculateRdkitFingerprints(
  mols,
  min_path = 1L,
  max_path = 7L,
  fp_size = 2048L,
  n_bits_per_hash = 2L,
  use_hydrogens = TRUE,
  target_density = 0,
  min_size = 128L,
  branched_paths = TRUE,
  use_bond_order = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{mols}{A character vector of SMILES or InChI strings, or a list of RDKit
Mol objects.}

\item{min_path}{An integer value. Minimum number of bonds to include in the subgraphs.}

\item{max_path}{An integer value. Maximum number of bonds to include in the subgraphs.}

\item{fp_size}{An integer value. Number of bits in the fingerprint.}

\item{n_bits_per_hash}{An integer value. Number of bits to set per path.}

\item{use_hydrogens}{A logical value. Include paths involving hydrogens in the fingerprint if
the molecule has explicit hydrogens.}

\item{target_density}{A numeric value. Fold the fingerprint until this minimum density has been
reached.}

\item{min_size}{An integer value. The minimum size the fingerprint will be folded to when
trying to reach \code{target_density}.}

\item{branched_paths}{A logical value. If set, both branched and unbranched paths will be used in
the fingerprint.}

\item{use_bond_order}{A logical value. If set, both bond orders will be used in the path hashes.}

\item{verbose}{A logical value. If \code{TRUE}, display warning and error messages emitted
by the RDKit backend. Default is \code{FALSE}.}
}
\value{
A matrix of integers (0 or 1) with one row per molecule and \code{fp_size}
  columns. Rows corresponding to invalid molecules contain \code{NA}.

  Additionally, the \code{"valid"} attribute is attached to indicate which
  molecules were successfully processed.
}
\description{
Calculate RDKit topological (path-based) fingerprints for a set of
  molecules.

  The fingerprints are calculated using the \code{RDKFingerprint()} function
  from the \code{rdkit.Chem.rdmolops} module in RDKit.

  Invalid molecules are represented by \code{NA} vectors. Row order is
  preserved so that the output aligns with the input.
}
\examples{
\dontshow{if (.IsRdkitAvailable(initialize = FALSE)) withAutoprint(\{ # examplesIf}
  # Calculate RDKit fingerprints
  smiles <- c("CCO", "c1ccccc1", "invalid_molecule")
  fps <- CalculateRdkitFingerprints(smiles)

  # Get the number of fingerprints (columns)
  ncol(fps)
  #> 2048

  # Check which molecules were successfully processed
  attr(fps, "valid")
  #> TRUE TRUE FALSE
\dontshow{\}) # examplesIf}
}
